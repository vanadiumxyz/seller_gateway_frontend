# Sourcerer

A local in-browser frontend to provide an easy UI for being a seller on the Vanadium network.

## Install Node.js

```
brew install node
```

## Run

Source all necessary credentials from `.env.example`

```
npm install
npm run dev
```

Got to the url and log in with the private key associated with your seller address (or the address that deployed the seller gateway smart contract)